import React from "react";

const TextUpperCaseNoBold = ({ title }) => {
  return (
    <div className="uppercase text-lg">
      <p>{title}</p>
    </div>
  );
};

export default TextUpperCaseNoBold;
