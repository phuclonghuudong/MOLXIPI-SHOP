import React from "react";

const TextBold = ({ title }) => {
  return (
    <div className="font-semibold uppercase text-lg ">
      <p>{title}</p>
    </div>
  );
};

export default TextBold;
